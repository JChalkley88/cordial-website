# build output
dist/
.vercel/
.astro/

# dependencies
node_modules/

# environment (secrets never committed)
.env
.env.local
.env.*.local

# documented design handoff — reference only, must never ship
design-reference/

# local draft working files — kept on disk, never tracked
drafts/

# logs and os files
npm-debug.log*
.DS_Store
Thumbs.db

# playwright test artifacts
.playwright-mcp/
